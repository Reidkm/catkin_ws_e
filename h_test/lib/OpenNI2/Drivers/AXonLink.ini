;---------------- AXonLink Driver Configuration -------------------
;
; NOTE: no spaces around '=' below
;

[Device]
; Stream Sync Threshold millisecond. Default is 100ms
SyncThresholdTime=200

; Stream Sync max frames in buffer. Default is 5.
; NOTE: Don't set this value too large, as extra RAM will be consumed when Sync is enabled. The estimated RAM size for each started stream is "SyncMaxFrameNum*frameSize".
SyncMaxFrameNum=5

; UsbControlDelay: USB control delay for each cmd, unit in ms. Default is 100. For windows only.

[Depth]

[Color]

[IR]
